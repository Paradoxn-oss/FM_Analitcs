[pytest]
# Garante que "import app", "from scouting..." funcionem quando o pytest é
# rodado a partir de qualquer diretório (não só da raiz do projeto).
pythonpath = .
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
